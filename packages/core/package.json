{
  "name": "@code-analyzer/core",
  "version": "1.0.0",
  "description": "Core analysis engine for code quality and review",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "test": "jest",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write src/**/*.ts",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "glob": "^10.3.10",
    "fs-extra": "^11.1.1",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/glob": "^8.1.0",
    "@types/fs-extra": "^11.0.4",
    "@types/jest": "^29.5.8",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.0"
  }
}
